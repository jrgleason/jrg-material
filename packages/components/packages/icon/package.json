{
  "name": "@jrg/icon-button",
  "version": "0.0.13",
  "description": "",
  "cjs": "dist/index.cjs",
  "browser": "dist/index.umd.js",
  "main": "dist/index.mjs",
  "scripts": {
    "lint": "eslint src --fix --ext mjs",
    "clean": "rm -rf dist",
    "build": "npm install && rollup -c && npm prune --production",
    "publish:patch": "npm run build && npm publish --access public && npm version patch",
    "upload": "npm publish --access public && npm version patch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jrg/base": "latest"
  }
}
